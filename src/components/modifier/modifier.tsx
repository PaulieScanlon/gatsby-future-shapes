import React, { FunctionComponent } from 'react'
import { Box } from 'theme-ui'

interface IModifierProps {}

export const Modifier: FunctionComponent<IModifierProps> = () => {
  return <Box>Modifier</Box>
}
